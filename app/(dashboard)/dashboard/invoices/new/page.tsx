import { InvoiceForm } from "@/components/invoices/InvoiceForm";

export const metadata = {
  title: "New invoice — PayFlow",
};

export default function NewInvoicePage() {
  return <InvoiceForm />;
}